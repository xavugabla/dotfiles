; Proxima currently shares the existing author identity. Keeping it in a
; separate fragment makes a later split low-risk.
[user]
	name = Xavugabla
	email = masterxavuga@gmail.com
	signingkey = {{ joinPath .chezmoi.homeDir ".ssh/id_ed25519.pub" }}
