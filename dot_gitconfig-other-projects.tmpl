; Keep the personal/non-DaisyChain identity explicit even before the
; dedicated macOS user exists.
[user]
	name = Xavugabla
	email = masterxavuga@gmail.com
	signingkey = {{ joinPath .chezmoi.homeDir ".ssh/id_ed25519.pub" }}
